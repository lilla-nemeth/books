'use client';

const HomePage = () => {
	return (
		<div className='flex w-full h-screen overflow-y-auto bg-slate-300 text-xl text-slate-600 text-center items-center justify-center font-semibold'>
			<h1 className='text-500 text-4xl font-semibold'>Sign In to Search Books</h1>
		</div>
	);
};

export default HomePage;
